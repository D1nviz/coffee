import styled from "styled-components";

const AppInfoBackground = styled.div`
display: flex;
justify-content: center;
`
const Text = styled.h2`
`
const AppInfo = () => {
   return (
      <>
         
      </>
   )
}

export default AppInfo;